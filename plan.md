create the style mechanic into get_html
the get_html function must render a html , and it should be able to render the html with a optional argument of a style
if the user passa a string as argument ,it means he wants to pass his custom style 
if the user not pass a string ,it should use the assets/default.css file (must be created)